# nexus-airgap-ops

Files-only Zarf package for the **terminal operator** path (Day 19).

Contains `nexus-tui` and the Day 19/14/15 probe scripts. No container images.
Does not include UDS Core, Athena, or Workbench.

Build via `../create-packages.sh` (compiles the TUI first).
