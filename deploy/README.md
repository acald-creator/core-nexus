# Deployment

This directory contains runtime deployment assets.

- `compose/` keeps local Docker Compose lab definitions.
- `kubernetes/` is reserved for near-term Kubernetes manifests and values.
- `uds/` is reserved for UDS and Zarf packages.
- `scripts/` keeps deployment and bootstrap scripts.
