apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexus-api-gateway
  namespace: soc
spec:
  template:
    spec:
      containers:
        - name: api-gateway
          env:
            - name: NEXUS_GW_ALERTS_SOURCE
              value: triage
