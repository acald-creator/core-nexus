# Vault/sync-vault-to-k8s owns nexus-gateway-secrets. Drop the console lab
# Secret from this overlay so Argo does not overwrite R2 credentials
# (prune: false leaves the live Secret in place).
$patch: delete
apiVersion: v1
kind: Secret
metadata:
  name: nexus-gateway-secrets
  namespace: soc
