apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ../../../system/tetragon
  - ../../../system/vector
  - ../../../system/kyverno
  - ../../../system/suricata
  - ../../wazuh

patches:
  - path: athena-gvisor-patch.yaml
  - path: ai-inference-patch.yaml
  - path: wazuh-dashboard-patch.yaml
# Vault is external — Shamir / file-backend labs live in nexus-hashistack recipe 04.
