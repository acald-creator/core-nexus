apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ../../../system/tetragon
  - ../../../system/vector
  - ../../../system/kyverno
  - ../../../system/suricata
  - ../../wazuh

patches:
  - path: athena-gvisor-patch.yaml
  - path: ai-inference-patch.yaml
# wazuh-dashboard-patch removed — dashboard not in this wazuh kustomize; secrets via wazuh-secrets.
# Vault is external — use nexus-hashistack + ./deploy/scripts/sync-vault-to-k8s.sh
