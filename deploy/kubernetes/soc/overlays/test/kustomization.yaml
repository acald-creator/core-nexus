apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ../../../system/tetragon
  - ../../../system/vector
  - ../../../system/kyverno
  - ../../../system/suricata
  - ../../wazuh

components:
  - ../../wazuh/secure

patches:
  - path: athena-gvisor-patch.yaml
  - path: ai-inference-patch.yaml
# Prefer lightweight: kubectl apply -k deploy/kubernetes/soc/overlays/wazuh-secure
# Prerequisites: generate-wazuh-indexer-certs.sh + sync-vault-to-k8s.sh
