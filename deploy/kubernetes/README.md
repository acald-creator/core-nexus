# Kubernetes Deployment

This directory is reserved for Kubernetes manifests, Kustomize bases, Helm values, and related deployment notes.

The near-term production-like path belongs here when it is not specifically UDS or Zarf packaging.
